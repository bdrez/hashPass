# hashPass
Built a Python CLI authentication system demonstrating password hashing, credential verification, and input validation. The user will enter their email and password, and the code will double check the entered password with the stored hash password to verify the user. It will also give the user an opportunity to create an account.

Features:
- Password hashing using MD5
- Credential verification for login
- Email format validation with regex
- Account creation for new users
